---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: spiffe-info
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spiffe-info
  labels:
    app: spiffe-info
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spiffe-info
  template:
    metadata:
      labels:
        app: spiffe-info
    spec:
      serviceAccountName: spiffe-info
      containers:
        - name: spiffe-info
          image: ghcr.io/mattiasgees/spiffe-info:latest
          ports:
            - name: http
              containerPort: 80
          env:
            - name: SPIFFE_ENDPOINT_SOCKET
              value: "unix:///tmp/swa-agent/public/api.sock"
            - name: PORT
              value: "80"
            - name: JWT_AUDIENCE
              value: "spiffe-info"
          volumeMounts:
            - name: swa-agent-socket
              mountPath: /tmp/swa-agent
              readOnly: true
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop: [ALL]
      volumes:
        - name: swa-agent-socket
          hostPath:
            path: /tmp/swa-agent
            type: Directory
---
apiVersion: v1
kind: Service
metadata:
  name: spiffe-info
  labels:
    app: spiffe-info
spec:
  selector:
    app: spiffe-info
  ports:
    - name: http
      port: 80
      targetPort: http
